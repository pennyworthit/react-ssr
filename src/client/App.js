import React from "react";

const styles = {
    textAlign: 'center',
    backgroundColor: 'red',
};

const App = () => (
    <div style={{backgroundColor: 'red'}}>🤨😒</div>
);

export default App;
